# CRM-QM Bearer token used by /api/submit-lead.js to push leads.
# Set the real value in Vercel Project Settings → Environment Variables.
# Never commit the actual token to this file or the repository.
CRM_API_TOKEN=
